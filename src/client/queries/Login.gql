mutation Login($username: String!, $password: String!, $clientId: String) {
    login(username: $username, password: $password, clientId: $clientId) {
        accessToken
        refreshToken
        expiredAt
    }
}
